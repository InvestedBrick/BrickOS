#include screen.brick

#include io.brick

brick main {
    clear_screen();
    write_string("Hello BrickOS!");
    newline();
    write_string("Updates coming soon :3");

    serial_setup_port(SERIAL_COM1_BASE,3);

    serial_write("Hello Serial Port!",SERIAL_COM1_BASE);
}